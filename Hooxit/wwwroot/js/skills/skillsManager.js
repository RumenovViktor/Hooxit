class SkillsManager {
    constructor(containerId, searchFieldId) {
        this.containerId = containerId;
        this.searchFieldId = searchFieldId;
    }

    search(skillName) {
        // TODO: Search Skills
    }
}